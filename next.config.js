/** @type {import('next').NextConfig} */
module.exports = {
    // output fully static HTML
    output: 'export',
  
    // create foldered pages (so /about/index.html exists)
    trailingSlash: true,
  }